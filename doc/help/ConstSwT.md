# ConstSwT

Constant value output when toggle switch is active, 0 when inactive.

## VALUE KNOB

Sets the constant value. Range: 0–127 units in steps of 1 unit.

## OUTPUT(S)

When the switch is active, the value set with the knob is output from the green numbered output. When inactive, the output is 0.

See also "Common Switch parameters".