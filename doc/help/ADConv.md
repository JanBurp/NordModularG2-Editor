# ADConv

Converts an incoming analog signal into an 8-bit digital representation.

## INPUT(S)

**In1** — The analog signal to be converted.

## OUTPUT(S)

**Out1–8** — The 8-bit digital representation of the analog input. Each output carries one bit of the binary value (Out1 = LSB, Out8 = MSB).